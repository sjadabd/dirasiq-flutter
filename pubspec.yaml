name: dirasiq
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.7.0
  get: ^4.6.6
  google_sign_in: ^6.1.5
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.3
  geolocator: ^12.0.0
  flutter_localizations:   # ✅ مهم عشان DatePicker واللغات
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
